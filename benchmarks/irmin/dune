(executable
	(name irmin_mem_rw)
	(modules irmin_mem_rw)
	(libraries irmin irmin-mem lwt lwt.unix))

(executable
	(name layers)
	(modules layers)
	(libraries irmin_pack irmin_layers index fmt logs lwt yojson unix cmdliner))

(alias (name buildbench) (deps layers.exe irmin_mem_rw.exe))
